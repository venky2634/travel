import 'package:flutter/material.dart';
import 'package:travel/navi.dart';

class BarItemPage extends StatelessWidget {
  const BarItemPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("home"),),
    );
  }
}